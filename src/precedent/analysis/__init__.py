"""The statistics layer. Everything here is computed from what ``sources/`` fetched.

Kept separate so that a number can always be traced to either an upstream field or an
arithmetic step in this package, never to a client that quietly aggregated something.
"""
